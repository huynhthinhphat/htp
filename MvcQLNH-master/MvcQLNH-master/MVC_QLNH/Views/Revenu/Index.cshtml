@using X.PagedList.Mvc.Core
@model X.PagedList.IPagedList<MVC_QLNH.Models.TbBillHistory>
@{
    ViewData["Title"] = "Index";
    var option = ViewData["CurrentFilter_option"];
    var searchString_startTime = ViewData["CurrentFilter_startTime"];
}
<style>
    td {
        text-align: center;
        background-color: #fff;
    }

    .table thead th {
        text-align: center;
        background-color: #f8f9fc;
    }

    .table {
        box-shadow: 0 .15rem 1.75rem 0 rgba(58, 59, 69, .15) !important;
    }

    #content_index {
        display: flex;
        flex-direction: column;
        padding: 0px 2%;
    }
</style>

<div id="content_index">
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">DOANH THU</h1>
    </div>
    <form asp-action="Index" method="get">
        <label>Tìm kiếm theo</label>
        @* <select name="SearchOption">
            @if (@option == "BillId" || @option == null)
            {

            <option value="BillId" selected>ID Hóa đơn</option>
                <option value="TableID">ID Bàn</option>
            }
            else if (@option == "TableID")
            {
                <option value="BillId">ID Hóa đơn</option>
            <option value="TableID" selected>ID Bàn</option>
            }
        </select> *@
        <select name="SearchOption">
            <option value="BillId" selected="@((ViewData["CurrentFilter_option"] as string) == "BillId")">ID Hóa đơn</option>
            <option value="TableID" selected="@((ViewData["CurrentFilter_option"] as string) == "TableID")">ID Bàn</option>
        </select>
        <input type="text" name="searchString_id" value="@ViewData["CurrentFilter_id"]" placeholder="Id tìm kiếm..."/>
        <div>
            @ViewBag.Message
        </div>
        <label>Thời gian từ</label>
        <input type="date" name="searchString_startTime" value="@((ViewData["CurrentFilter_startTime"] as DateOnly?)?.ToString("yyyy-MM-dd"))" />
        <label>đến</label>
        <input type="date" name="searchString_endTime" value="@((ViewData["CurrentFilter_endTime"] as DateOnly?)?.ToString("yyyy-MM-dd"))" />
        <input type="submit" value="Lọc" />
    </form>
    <a asp-action="Index">Back to Full List</a>

<table class="table">
    <thead>
        <tr>
            <th>
                ID HÓA ĐƠN
            </th>
            <th>
                ID BÀN
            </th>
            <th>
                TỔNG TIỀN
            </th>
            <th>
               THỜI GIAN
            </th>
            <th>
                NHÂN VIÊN
            </th>
            <th>Chức năng</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <tr>
            <td>
               @Html.DisplayFor(modelItem => item.BillId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Table.TableId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BillDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserInfo.FullName)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.BillId">Chi tiết</a>
                <form asp-action="Delete" asp-route-id="@item.BillId" method="post" onsubmit="return confirm('Bạn có chắc chắn muốn xóa hóa đơn này không?');">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
    <!-- Phân trang -->
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <!-- Phân trang -->
                <nav aria-label="Page navigation">
                    <ul class="pagination justify-content-center">
                        @if (Model.PageCount > 1)
                        {
                            for (int i = 1; i <= Model.PageCount; i++)
                            {
                                <li class="page-item @(Model.PageNumber == i ? "active" : "")">
                                    <a class="page-link" href="@Url.Action("Index", new { page = i })">@i</a>
                                </li>
                            }
                        }
                    </ul>
                </nav>
            </div>
        </div>
    </div>

    <!-- Hiển thị số trang và số món -->
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <p>Số trang: @Model.PageCount</p>
                <p>Số hóa đơn: @Model.TotalItemCount</p>
            </div>
        </div>
    </div>
</div>
